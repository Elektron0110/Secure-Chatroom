[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Messenger — веб-мессенджер на Python/Flask"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
    "flask-sock>=0.7.0",
    "sqlalchemy>=2.0.0",
    "simple-websocket>=1.1.0",
]
